============
Installation
============

At the command line::

    $ pip install gerrit-dashboard-nfv

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv gerrit-dashboard-nfv
    $ pip install gerrit-dashboard-nfv